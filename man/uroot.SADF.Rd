% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uroot.SADF.R
\name{uroot.SADF}
\alias{uroot.SADF}
\alias{uroot.GSADF}
\title{Supremum ADF tests}
\usage{
uroot.SADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = TRUE,
  add.p.value = TRUE,
  boot.p = FALSE,
  boot.iter = 999
)

uroot.GSADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = TRUE,
  add.p.value = TRUE,
  boot.p = FALSE,
  boot.iter = 999
)
}
\arguments{
\item{y}{A time series of interest.}

\item{trim}{A trimming parameter to determine the lower and upper bounds for
a possible break point.}

\item{const}{Whether the constant needs to be included.}

\item{add.p.value}{Whether the p-value is to be returned. This argument is
needed to suppress the calculation of p-values during the precalculation of
tables needed for the p-values estimating.}
}
\value{
An object of type \code{sadf}. It's a list of:
\itemize{
\item \code{y},
\item \code{trim},
\item \code{const},
\item vector of \eqn{t}-values,
\item the value of the corresponding test statistic,
\item \eqn{p}-value if it was asked for.
}
}
\description{
Supremum ADF tests
}
\details{
\code{SADF.test} is a test statistic equal to the minimum value of \link{uroot.ADF} for
subsamples starting at \eqn{t = 1}.

\code{GSADF.test} is a generalized version of \code{SADF.test}. Subsamples are allowed
to start at any point between 1 and \eqn{T (1 - trim)}.
}
\references{
Kurozumi, Eiji, Anton Skrobotov, and Alexey Tsarev.
“Time-Transformed Test for Bubbles under Non-Stationary Volatility.”
Journal of Financial Econometrics, April 23, 2022.
https://doi.org/10.1093/jjfinec/nbac004.
}
