% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uroot.SADF.R, R/uroot.STADF.R,
%   R/uroot.sb.GSADF.R, R/uroot.w.SADF.R
\name{uroot.SADF}
\alias{uroot.SADF}
\alias{uroot.GSADF}
\alias{uroot.STADF}
\alias{uroot.GSTADF}
\alias{uroot.sb.GSADF}
\alias{uroot.w.SADF}
\alias{uroot.w.GSADF}
\title{Supremum ADF tests}
\usage{
uroot.SADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = TRUE,
  add.p.value = TRUE,
  boot.p = FALSE,
  boot.iter = 999
)

uroot.GSADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = TRUE,
  add.p.value = TRUE,
  boot.p = FALSE,
  boot.iter = 999
)

uroot.STADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = FALSE,
  omega.est = TRUE,
  truncated = TRUE,
  is.reindex = TRUE,
  ksi.input = "auto",
  hc = 1,
  pc = 1,
  add.p.value = TRUE
)

uroot.GSTADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = FALSE,
  omega.est = TRUE,
  truncated = TRUE,
  is.reindex = TRUE,
  ksi.input = "auto",
  hc = 1,
  pc = 1,
  add.p.value = TRUE
)

uroot.sb.GSADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = TRUE,
  alpha = 0.05,
  boot.iter = 999,
  urs = TRUE,
  seed = round(10^4 * sd(y))
)

uroot.w.SADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = TRUE,
  alpha = 0.05,
  iter = 4 * 200,
  urs = TRUE,
  seed = round(10^4 * sd(y))
)

uroot.w.GSADF(
  y,
  trim = 0.01 + 1.8/sqrt(length(y)),
  const = TRUE,
  alpha = 0.05,
  iter = 4 * 200,
  urs = TRUE,
  seed = round(10^4 * sd(y))
)
}
\arguments{
\item{y}{A time series of interest.}

\item{trim}{A trimming parameter to determine the lower and upper bounds for
a possible break point.}

\item{const}{Whether the constant needs to be included.}

\item{add.p.value}{Whether the p-value is to be returned. This argument is
needed to suppress the calculation of p-values during the precalculation of
tables needed for the p-values estimating.}

\item{boot.iter}{Number of bootstrapping iterations.}

\item{omega.est}{Whether the variance of Nadaraya-Watson residuals should be
used.}

\item{truncated}{Whether the truncation of Nadaraya-Watson residuals is
needed.}

\item{is.reindex}{Whether the Cavaliere and Taylor (2008) time transformation
is needed.}

\item{ksi.input}{The value of the truncation parameter. Can be either \code{auto}
or the explicit numerical value. In the former case the numeric value is
estimated.}

\item{hc}{The scaling parameter for Nadaraya-Watson bandwidth.}

\item{pc}{The scaling parameter for the estimated truncation parameter value.}

\item{alpha}{A significance level of interest.}

\item{urs}{Use \verb{union of rejections} strategy.}

\item{seed}{A seed parameter for the random number generator.}

\item{iter}{Number of iterations.}
}
\value{
\link{uroot.GSADF} returns an object of class \code{bt_SADF} and subclass \code{bt_GSADF}.

\link{uroot.STADF} returns an object of type \code{bt_SADF} and subclass \code{bt_STADF}.
It's a list of:
\item{y}{}
\item{N}{Number of observations,}
\item{trim}{}
\item{const}{}
\item{omega.est}{}
\item{truncated}{}
\item{is.reindex}{}
\item{new.index}{the vector of new indices,}
\item{ksi.input}{}
\item{hc}{}
\item{h.est}{}
\item{u.hat}{}
\item{pc}{}
\item{w.sq}{}
\item{t.values}{vector of \eqn{t}-values,}
\item{*.value}{the value of the corresponding test statistic,}
\item{u.hat.truncated}{truncated residuals if truncation was asked for,}
\item{ksi\verb{, }sigma}{estimated values of the truncation parameter and resulting s.e. if \code{ksi.input} equals \code{auto},}
\item{eta.hat}{the values of reindexing function if reindexing was asked for,}
\item{\eqn{p}-value}{if it was asked for.}

\link{uroot.GSTADF} returns an object of type \code{bt_SADF} and subclass \code{bt_GSTADF}.

\link{uroot.sb.GSADF} returns an object of class \code{bt_SADF} and subclass \code{bt_sbGSADF}. It's a list of:
\itemize{
\item \code{y},
\item main parameters, such as \code{trim}, \code{const}, \code{alpha}, \code{iter},
\item \code{seed},
supremum SBADF-statistic values incl. bootstrapped ones,
\item p-value,
\item indicator of explosive process.
}

If \code{urs=TRUE} then some additional values included:
\itemize{
\item t-values,
\item GSADF bootstrapped statistics,
\item bootstrapped U-values.
}

\link{uroot.w.SADF} returns an object of class \code{bt_SADF} and subclass \code{bt_wSADF}.
It's a list of:
\item{y}{}
\item{trim}{}
\item{const}{}
\item{alpha}{}
\item{iter}{}
\item{urs}{}
\item{seed}{}
\item{sigma.sq}{the estimated variance,}
\item{BZ.values}{a series of BZ-statistic,}
\item{supBZ.value}{the maximum of \code{supBZ.values},}
\item{supBZ.bootstsrap.values}{bootstrapped supremum BZ values,}
\item{supBZ.cr.value}{supremum BZ \eqn{\alpha} critical value,}
\item{p.value}{},
\item{is.explosive}{1 if \code{supBZ.value} is greater than \code{supBZ.cr.value}.}

if \code{urs} is \code{TRUE} the following items are also included:
\item{t.values}{vector of \eqn{t}-values,}
\item{SADF.value}{the value of the SADF test statistic,}
\item{SADF.bootstrap.values}{bootstrapped SADF values,}
\item{SADF.cr.value}{critical value of \code{SADF.value}.}
\item{U.value}{union test statistic value,}
\item{U.bootstrap.values}{bootstrapped series of \code{U.value},}
\item{U.cr.value}{critical value of \code{U.value}.}

\link{uroot.w.GSADF} returns an object of type \code{bt_SADF} and subclass \code{bt_wGSADF}.

\link{uroot.SADF} returns an object of type \code{bt_SADF}. It's a list of:
\itemize{
\item \code{y},
\item \code{trim},
\item \code{const},
\item vector of \eqn{t}-values,
\item the value of the corresponding test statistic,
\item \eqn{p}-value if it was asked for.
}
}
\description{
Supremum ADF tests
}
\details{
Tests with time transformation are the modified versions of
the ordinary \code{SADF} and \code{GSADF} tests using Nadaraya-Watson residuals and
reindexing procedure by Cavaliere and Taylor (2008).

Refactored original code by Kurozumi et al.

This set of unit root tests are developed for testing for bubbles
under time-varying non-stationary volatility.
Because the limiting distribution of the seminal Phillips et al. (2011) test
depends on the variance function and usually requires a bootstrap implementation
under heteroskedasticity, Kurozumi et al. (2022) construct the test
based on a deformation of the time domain.
The proposed test is asymptotically pivotal under the null hypothesis
and its limiting distribution coincides with that of the standard test
under homoskedasticity, so that the test does not require computationally
extensive methods for inference.

\code{uroot.SADF} is a test statistic equal to the minimum value of \link{uroot.ADF} for
subsamples starting at \eqn{t = 1}.

\code{GSADF.test} is a generalized version of \code{SADF.test}. Subsamples are allowed
to start at any point between 1 and \eqn{T (1 - trim)}.
}
\references{
Cavaliere, Giuseppe, and A. M. Robert Taylor.
“Time-Transformed Unit Root Tests for Models with Non-Stationary Volatility.”
Journal of Time Series Analysis 29, no. 2 (March 2008): 300–330.
https://doi.org/10.1111/j.1467-9892.2007.00557.x.

Harvey, David I., Stephen J. Leybourne, and Yang Zu.
“Sign-Based Unit Root Tests for Explosive Financial Bubbles
in the Presence of Deterministically Time-Varying Volatility.”
Econometric Theory 36, no. 1 (February 2020): 122–69.
https://doi.org/10.1017/S0266466619000057.

Kurozumi, Eiji, Anton Skrobotov, and Alexey Tsarev.
“Time-Transformed Test for Bubbles under Non-Stationary Volatility.”
Journal of Financial Econometrics, April 23, 2022.
https://doi.org/10.1093/jjfinec/nbac004.
}
